# Changelog

## v1.2.0 · 2026-09-21

**ROI 预警从「每小时出一份表」变成「半小时播一次报」，并且把一个一直在偷偷少报数字的坑修了。**

### 新增：运营播报（`node src/index.js board`）

每半小时把当天各推广系列的 成本 / 成交 / ROI 推到飞书群，另给一栏「近 1 小时」增量。

- 数字取自广告后台**计划列表页接口返回的计划级官方数字**，不再由素材层加总 —— 之前日报对不上就栽在「素材层合计 ≠ 计划级数字」上
- 「近 1 小时」是两次快照相减，快照按市场当地日期分组，跨天不相减
- 商品计划与直播计划是两张分开的列表，列顺序也不同，各读一次后合并
- 开关：`config.json` 的 `feishu.board.enabled`，或 `board --on` / `board --off`（下一轮生效，不用重启）

### 修复：直播计划长期取不到数

根因不在列名、不在认表，在导航判断的一行老代码：`gotoWithRetry` 有个「虽然报超时，但其实已经跳到目标页了」的兜底，靠 URL 里的 `campaign_id` 判断。素材页有 `campaign_id` 所以一直没出事，而**计划列表页压根没有** → 这个判断恒为真。于是切 tab 超时时它谎报成功，上层读到的还是上一张表，认表不符判失败，而且 3 次重试一次都没跑 —— 每次都是同一边挂到底。

现在列表页要求 **路径 + type + 起止日期** 全部对上才算到位，切 tab 前先把页面拽回空白页，上一个 tab 的表格不会留在 DOM 里。

### 修复：表格边渲染边读，数字会少一截

实测发现列表行是一条一条冒出来的（0 行 → 3 行）。原来只要看到 1 行就开始读，正好撞上渲染到一半就只读到一部分。这种错**不报错**，比取不到更坑。现在行数要连续 2.5 秒不变才算稳。

配套的耐心策略：每个 tab 最多 5 次、退避 20 → 40 → 60 → 80 秒、单 tab 5 分钟预算；整轮「取全才推」，商品和直播都齐才推群 / 写表 / 存快照，不齐隔 90 秒整轮重来；看门狗从 6 分钟放宽到 12 分钟，免得把正在重试的那轮掐死。

### 新增：自动发现广告计划（`discover`）

拦截列表页自己的 JSON 接口拿 `campaign_id` / `product_id`，合并进 `campaigns.json`。不扒 DOM —— 列表页的按钮多是 JS 驱动的，`href` 里未必有 id。抓不到也不猜，会把捕获到的接口清单打出来。

### 新增：运行日志回传飞书（`logsync.js`）

WARN / ERROR 加几条关键 INFO 写进多维表格「运行日志」表，人不在那台常开的 Windows 旁边，也能看它几点几分卡在哪一步。纯旁路：写表失败一律吞掉不影响扫描，断网时留在队列里下次补发（最多 500 条）。

### 新增：暴力测试（`stress` / `暴力测试.bat`）

造一个**故意抽风**的假后台，商品与直播各随机抽一种毛病，反复捶打取数链路：正常 / 永不响应 / 前两次不响应 / 晚几秒渲染 / 行一条条冒 / 返回另一张表 / 有表没行 / 服务器报错 / 页面被关掉。

只认一条铁律：**要么完整正确，要么明确失败** —— 绝不串台、绝不少行、绝不拿 0 冒充。不用登录、不碰真实数据，约 6 分钟跑 18 轮（故障组合每轮随机抽，项数略有浮动；本机实测 247 项全过，0 失败）。CI 里也会跑 6 轮。

### 其它

- 离线自检 113 项 → **226 项**
- Windows 侧新增六个 `.bat`：播报-预览 / 播报-开启自动 / 播报-关闭自动 / 播报-诊断 / 发现新计划 / 暴力测试
- 脱敏脚本补齐：多维表格数据表 ID（出现在任何位置，含正文与 URL）、测试夹具里混进来的真实推广系列与商品 ID、`campaigns` 的手工备份副本、Windows 侧的内部版本说明

## v1.1.0 · 2026-09-20

**从「越南专用」变成「东南亚通用框架」。**

- **多市场**：越南 / 泰国 / 印尼 / 马来西亚 / 菲律宾 / 新加坡。市场参数集中在一处，换国家改配置不改代码
  - ROI 预警：`config.json` 新增 `market` 块（国家码、货币、符号、时区、汇率），六国模板见 `markets.example.json`；旧的 `vndToCnyRate` / `timezoneOffsetHours` 仍兼容，`node src/index.js config` 可自动迁移
  - 表头识别扩到七种语言（中 / 英 / 越 / 泰 / 印尼 / 马来 / 菲律宾），数字解析自动判别点逗号，小面额币种（泰铢 / 林吉特 / 新币）无符号时按本币算
  - 主播排名：`config.json` 新增 `currency` 与 `local_lang`，第三张工作表的语言与货币可整体替换（示例给的是泰语）
- **汇率插件 v2.0**（目录 `vnd-cny-extension` → `fx-extension`）：重写成「市场表 + 纯函数内核 + DOM 层」三层
  - 六国货币、本地缩写（Tr / Tỷ / jt / rb / K / M / B）、符号前后缀、点逗号千分位全部覆盖
  - 按域名自动识别市场，可切换换算成人民币或美元
  - 加国家只改 `markets.js` 一段
  - 新增 `test/` 单元测试与 `demo/` 演示页（README 的前后对比图就是它）
- **CI**：GitHub Actions 跑插件单元测试、ROI 预警离线自检（113 项）、全仓库语法检查
- 脱敏脚本覆盖飞书租户域名、多维表格 token、云盘文件夹 ID

## v1.0.0 · 2026-09-20

首个公开版本。

- 五个子项目整合进一个仓库：feishu-api、roi-monitor（Mac / Windows）、host-ranking、vnd-cny-extension
- **可复用**：所有业务参数（表 ID、负责人、店铺与计划映射、实习生名单、阈值、汇率、品牌名、Google 表 ID、列索引）全部抽到 `*.json` 配置，代码零改动即可接入其他店铺；每处配置附 `*.example.json` 模板
- 中英双语 README，按角色导航，「10 分钟接入」步骤表
- `sync-from-desktop.sh` + `scrub.sh`：从开发机同步源码并自动脱敏
- GitHub Actions 打包 host-ranking 的 Windows EXE
- MIT 许可
